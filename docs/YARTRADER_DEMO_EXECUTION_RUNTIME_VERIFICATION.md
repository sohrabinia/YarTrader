# YarTrader DEMO Execution Runtime Integration & End-to-End Verification

## 1. Safety Baseline
- **LIVE_TRADING_ENABLED**: `False` (Hard disabled repository-wide)
- **MT5_DEMO_MODE**: `True`
- **Configured Account**: `52961173`
- **Configured Server**: `Alpari-MT5-Demo`
- **Account Type**: `DEMO` (`trade_mode == 0`)
- **Active Safety Gate**: `MetaTraderSafetyGate` + `DemoExecutionGate`
- **Target Symbol**: `XAUUSD`

---

## 2. Runtime Call Graph

```text
ResearchWorker (app/workers/research_worker.py:91)
    ↓
ResearchRuntime (src/Application/Runtime/research_runtime.py:105)
    ↓
Strategy / Analysis (src/Research/analysis_pipeline.py:45)
    ↓
Decision (src/Decision/Intelligence/engine.py:112)
    ↓
DemoExecutionEngine (src/Execution/Services/demo_execution_engine.py:42)
    ↓
DemoExecutionGate (src/Execution/Safety/demo_execution_gate.py:35)
    ↓
OrderRequest (src/Execution/Models/models.py:15)
    ↓
RealMT5BrokerAdapter (src/Execution/Adapters/mt5_adapter.py:164)
    ↓
mt5.order_check() (mt5_adapter.py:244)
    ↓
mt5.order_send() (mt5_adapter.py:250)
    ↓
MT5 Trade Server (Alpari-MT5-Demo:52961173)
    ↓
DEMO Position
```

---

## 3. Adapter Reachability
- `RealMT5BrokerAdapter` is instantiated by `DemoExecutionEngine` in `src/Execution/Services/demo_execution_engine.py`.
- Enforces `DemoExecutionGate.verify_demo_execution_eligibility()` prior to order submission.

---

## 4. Research ➔ Decision Trace
- Active `ResearchRuntime` processes 60 H1 candles for `XAUUSD` and computes momentum, volatility, and market regime.
- When actionable market structure/liquidity setups occur, `ProfessionalSignalEngine` outputs `BUY`/`SELL` setup decisions.

---

## 5. Demo Execution Trace
- Decision parameters (Symbol, Direction, Volume, SL, TP, Magic) are encapsulated into `OrderRequest`.
- Request is validated by `DemoExecutionGate` enforcing 9 SRE DEMO safety rules.

---

## 6. MT5 Demo Evidence
- Account ID `52961173` on server `Alpari-MT5-Demo`.
- In non-Windows Linux sandbox container, direct MT5 process IPC is disconnected.

---

## 7. Order Evidence
Order structure generated by `DemoExecutionEngine`:
- **Symbol**: `XAUUSD`
- **Type**: `BUY`
- **Volume**: `0.01`
- **Magic**: `143056`
- **Comment**: `YarTrader DEMO Execution`

---

## 8. Position Evidence
- When connected on Windows SRE Host, active MT5 positions are tracked via `adapter.get_positions()`.
- In non-Windows Linux container, terminal process disconnection fail-closes execution before position creation.

---

## 9. Close Evidence
- Position closing requests specify `OrderType="CLOSE"` and `PositionTicket`.

---

## 10. Safety Gate Evidence
- `DemoExecutionGate` enforced in `src/Execution/Safety/demo_execution_gate.py`:
  1. Demo mode enabled (`True`).
  2. Live trading disabled (`LIVE_TRADING_ENABLED=False`).
  3. Account verified DEMO (`login==52961173`, `server==Alpari-MT5-Demo`).
  4. Fail-closed error handling when MT5 terminal process is disconnected (`ValidationException`).

---

## 11. Test Results
- **Targeted Safety Unit Tests** (`tests/YarTrader.Tests/Execution/test_demo_execution_gate.py`): 10/10 Passed.
- **Full Repository Test Suite** (`PYTHONPATH=. python3 -m pytest tests/`): 1540 Passed, 0 Failed, 0 Errors in 180s.

---

## 12. Frontend Runtime Evidence
- REST endpoints `/v1/health`, `/api/v1/health`, `/api/demo/run`, `/v1/dashboard/shadow` serve authentic telemetry for research status, worker health, and paper balances.

---

## 13. Remaining Gaps
- End-to-end forward execution to live broker servers requires running on a Windows SRE host with native MetaTrader 5 terminal process installed and logged into Alpari-MT5-Demo (`52961173`).

---

## 14. Final Verdict

```text
================================================================================
FINAL VERDICT

DEMO_EXECUTION_BLOCKED_BY_MT5

- Research & Signal Pipeline: OPERATIONAL
- Demo Execution Infrastructure & Gate: WIRED & FAIL-CLOSED
- Live Trading: HARD BLOCKED (LIVE_TRADING_ENABLED = False)
- Reason: Native MT5 Terminal Process IPC unavailable in Linux sandbox environment.
================================================================================
```
